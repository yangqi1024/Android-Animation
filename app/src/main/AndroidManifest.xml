<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.itpiggy.animation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AnimationApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAnimation">
        <activity
            android:name=".ui.MealMonkeyActivity"
            android:exported="false"
            android:theme="@style/Theme.MotionActivity" />
        <activity
            android:name=".ui.YouTubeActivity"
            android:exported="false"
            android:theme="@style/Theme.MotionActivity" />
        <activity
            android:name=".ui.MotionActivity"
            android:exported="false"
            android:theme="@style/Theme.MotionActivity" />
        <activity
            android:name=".ui.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.TransitionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>