<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidAnimation" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:windowSharedElementEnterTransition" >@transition/grid_detail_transition</item>
        <item name="android:windowSharedElementExitTransition" >@transition/grid_detail_transition</item>
    </style>
    <style name="Theme.MotionActivity" parent="Theme.AppCompat.Light.NoActionBar"/>
</resources>